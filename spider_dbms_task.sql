create database InventoryDB;

use InventoryDB;

-- for creating table 

create table products(
serial_no int primary key,
Name_ varchar(20),
Description_ varchar(30),
price int,
quantity int,
category varchar(20)
);

-- now inserting data

insert into products values(
1, 'ASUS_TUF_GAMING','HIGH_STORAGE_GAMING_LAPTOP',70000,1,'LAPTOP');

insert into products values(
2, 'SAMSUNG_M14_5G','HIGH_STORAGE_MOBLIE',15000,3,'MOBLIE');

insert into products values(
3, 'APPLE_AIRPODS_PRO2','PROWERFUL_CLEAR_SOUND',13000,5,'EARPODS');

-- retrieve all products
select * from products ;

-- retrieve products with less price

select * from products 
where price < 14000;

-- retrieve products with more than specified quantity

select * from products 
where quantity > 1;


-- for update the table

set SQL_SAFE_UPDATES=0;
update products 
set price = 68999
where price = 70000;

select * from products


-- for deleting a products
set SQL_SAFE_UPDATES=0;

delete from products
where serial_no = 2;
select * from products;




















































































































































































































































































































































































































































































































DESCRIBE student_table;
